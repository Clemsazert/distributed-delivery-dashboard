import styled from 'styled-components';

export const HeadPageTitle = styled.h1`
  font-size: ${props => props.theme.fontSizes.big};
  margin-top: ${props => props.theme.gridUnit}px;
  margin-bottom: ${props => props.theme.gridUnit}px;
`;